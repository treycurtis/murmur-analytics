name: murmur_dbt
version: "1.0.0"
config-version: 2

profile: murmur

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  murmur_dbt:
    raw:
      +schema: raw
      +materialized: table
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
